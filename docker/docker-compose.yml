version: '3.5'
services:
  py:
    build: ./gian_trade_engine/
    container_name: docker_py_1 
    restart: always
    volumes:
      - ../python/GianTrade_Engine/app:/usr/src/app
    networks:
      - backend
    ports:
      - "10001:5000"
#   remove after development
    command: /bin/sh
    stdin_open: true
    tty: true
#   \remove after developmet
    depends_on:
      - db
  
  db:
    image: postgres:12.0
    container_name: docker_db_1
    restart: always
    environment:
      POSTGRES_PASSWORD: pass
      POSTGRES_USER: myuser
      POSTGRES_DB: db
    volumes:
      - ./gian_trade_db/data:/var/lib/postgresql/data
    networks:
      - backend
    ports:
      - "10000:5432"

networks:
  backend:
    name: gian_trade_net
